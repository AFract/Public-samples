{
  "BackgroundExecution":
  {
    // Global
    // Specify if the process only runs one pending query and exits immediately after. Default FALSE when it is used as a Windows Service.
    "ExecuteOnceAndExit": false,
    // Interval between each lookup for a new pending task
    "WaitTimeInSeconds": 10
  }
}
